[[Python]]

1) Сортировка по возрастанию, чтобы поменять - использовать `reverse=True` как аргумент
2) Сортировка массива по определенному элементу. Например, массив `[['hello',23],['hi',15]]` нужно отсортировать по второму элементу. Используем `mas.sort(key=lambda x: x[1])` лямбда-функцию.